#include <iostream>

using namespace std;
int main()
{
	int T, N, num;

	cin >> T;
	for (int i = 0; i < T; i++)
	{
		cin >> N;
		int total = 0;
		for (int i = 0; i < N; i++)
		{
			cin >> num;
			total += num;
		}
		cout << total << endl;
	}
	return 0;
}
